- name: install nodejs
  #become: true
  package:
    name: nodejs

- name: install npm
  #become: true
  package:
    name: npm
  notify:
    - set node_modules prefix
    - set npm_cache

- name: install yarn
  #become: true
  package:
    name: yarn
